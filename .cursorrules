You are working with a React Native (Expo) architecture reference project. Read the skill files before generating any code.

# Architecture Reference

All architecture documentation is in `skills/arc-skill/`. Read these files BEFORE writing code:

- `project-structure.md` — Folder tree, naming conventions, barrel exports
- `navigation.md` — React Navigation 7.x (native-stack + bottom-tabs)
- `theme.md` — Theme system, color schemes, useStyles hook
- `components.md` — Component file structure (types, styles, constants in separate files)
- `api-services.md` — Axios HTTP client with auth interceptors
- `storage.md` — MMKV storage wrappers
- `state-management.md` — TanStack React Query + persistence
- `performance.md` — React Native performance best practices
- `providers.md` — Provider stack (GestureHandler → Query → Theme → SafeArea → i18n → Nav → Toast)
- `typescript.md` — TSConfig, path aliases, type conventions
- `i18n.md` — Lingui.js internationalization

# Mobile Design System

Read `skills/arc-skill/mobile-design/` for:
- Touch psychology (44pt min targets, thumb zones, haptics)
- Platform conventions (iOS HIG vs Material Design 3)
- Performance optimization (FlatList, animations, memory)
- Typography (Dynamic Type, system fonts, type scales)
- Color system (OLED, dark mode, semantic colors, contrast)
- Navigation patterns (tab bar, stack, deep linking)
- Backend patterns (offline sync, push notifications, auth)
- Design thinking (anti-memorization, context-based decisions)

# Code Templates

Use templates from `skills/arc-skill/templates/`:
- `component.md` — Component scaffolding
- `screen.md` — Screen scaffolding
- `hook.md` — Custom hook patterns
- `api-service.md` — API module + React Query hooks

# Critical Rules

1. Component structure: index.tsx + name.styles.ts + name.types.ts + name.constants.ts
2. All styles via `useStyles()` hook — never inline StyleSheet.create
3. All sizing via `react-native-size-matters` moderateScale — never hardcode pixels
4. All API data via React Query hooks — never call API directly in components
5. All storage via typed MMKV wrappers — never access MMKV directly
6. Navigation typed via `useAppNavigation()` hook
7. Barrel exports (index.ts) in every folder
8. kebab-case folders, PascalCase exports
9. Touch targets minimum 44pt (iOS) / 48dp (Android)
10. FlatList for lists — never ScrollView for dynamic/long lists
11. Reanimated for animations (UI thread), never Animated API for complex animations
12. expo-image instead of React Native Image
13. Platform-respectful: iOS feels iOS, Android feels Android
14. Dark mode: #121212 surfaces, #E8E8E8 text, WCAG AA contrast
15. No console.log in production — blocks JS thread
16. React Compiler enabled — do NOT manually add React.memo, useCallback, or useMemo
17. Clean up timers, listeners, subscriptions in useEffect return
18. Install deps via `npx expo install`, then `npx expo doctor`
